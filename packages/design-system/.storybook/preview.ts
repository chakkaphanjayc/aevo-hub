import "../src/tokens.css";
import "../src/styles.css";

const preview = {
  parameters: {
    layout: "centered",
    controls: { expanded: true },
    a11y: { test: "todo" }
  },
  tags: ["autodocs"]
};

export default preview;
